<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bakery.dam.androidtpv">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.activities.login.LoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activities.main.MainActivity" />
        <activity android:name=".controller.activities.Crear.CrearTipo" />
        <activity
            android:name=".controller.activities.Crear.CrearTicket"
            android:label="@string/title_activity_crear_ticket"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".controller.activities.main.MenuActivity"
            android:label="Bakery TPV"
            android:theme="@style/AppTheme"></activity>
            android:theme="@style/AppTheme" />
        <activity android:name=".CreacionTicketActivity"></activity>
    </application>

</manifest>